export namespace TowTruckTypeApplicationEvent {
  export namespace TowTruckTypeCreated {
    export const key = 'towTruckType.application.towTruckType.created'

    export type Payload = {
      id: string
      userId: string
    }
  }
}
